import buildMongoDbRealmDataProvider from "./src";
import {
  cleanObj,
  cleanArray,
  cleanValue,
  preparseDocument,
  preparsePath,
  oid,
} from "./src/utils";

export default buildMongoDbRealmDataProvider;

export {
  cleanObj,
  cleanArray,
  cleanValue,
  preparseDocument,
  preparsePath,
  oid,
};
